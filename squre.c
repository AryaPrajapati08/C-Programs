#include <stdio.h>

void main()
{
    float l,a,p;

    printf("Enter the value of length of squre :");
    scanf("%f",&l);

    a = l * l;

    p = 4 * l;

    printf("Area of the squre is : %f \n", a);

    printf("Parameter of the squre is : %f", p);

}